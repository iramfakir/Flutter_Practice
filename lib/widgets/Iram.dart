import 'package:flutter/material.dart';

class Iram extends StatelessWidget{
@override
Widget build(BuildContext context){
  return  const Center(
    child: Text(
      'Iram Fakir',
      textDirection: TextDirection.ltr,
      style: TextStyle(color: Colors.pink),
    ),
  );
}
}